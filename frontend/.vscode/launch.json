{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug CRA in Chrome",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/src",
      "sourceMaps": true,
      "preLaunchTask": "npm: start"
    },
    {
      "name": "Debug CRA in Edge",
      "type": "pwa-msedge",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/src",
      "sourceMaps": true,
      "preLaunchTask": "npm: start"
    }
  ],
  "compounds": [
    {
      "name": "Start dev server + Chrome",
      "configurations": ["Debug CRA in Chrome"]
    }
  ]
}
