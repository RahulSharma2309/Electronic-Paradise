---
# Product Service Kubernetes Service for Staging
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: staging
  labels:
    app: product-service
    environment: staging
    managed-by: electronic-paradise
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: product-service
    environment: staging

---
# Product Service Kubernetes Service for Production
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: prod
  labels:
    app: product-service
    environment: production
    managed-by: electronic-paradise
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: product-service
    environment: production
